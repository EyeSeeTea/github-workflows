name: "Master Workflow"

on:
  workflow_call:
    inputs:
      languages:
        description: "Languages to analyze"
        required: false
        default: '["javascript-typescript"]'
        type: string
      build_mode:
        description: "Build mode for CodeQL analysis"
        required: false
        default: "none"
        type: string
      bundlemonrc_branch_name:
        description: "Branch where to fetch bunlemonrc" # couldn't find a way to get the workflow branch from caller. Need to pass for testing
        required: false
        default: "master"
        type: string
      runner:
        description: "Specify runner for general jobs (tests, codeql, build)"
        required: false
        default: "general-runner"
        type: string
      dependency_track_runner:
        description: "Specify runner for Dependency Track workflows"
        required: false
        default: "dependency-track-runner"
        type: string
      workflows_to_run:
        description: 'Comma-separated list of workflows to run (e.g., flow1,flow2)'
        required: false
        default: 'all' #'run-tests,codeql-scan,build-check'
        type: string
    secrets:
      DTRACK_API_KEY:
        required: false

jobs:
  detect-project-type:
    if: contains(inputs.workflows_to_run, 'dependency-track') || contains(inputs.workflows_to_run, 'all')
    runs-on: ${{ inputs.runner }}
    outputs:
      is_node: ${{ steps.check.outputs.is_node }}
    steps:
      - uses: actions/checkout@v4
      - id: check
        run: |
          if [ -f package.json ]; then
            echo "is_node=true" >> $GITHUB_OUTPUT
          else
            echo "is_node=false" >> $GITHUB_OUTPUT
          fi

  detect-yarn4:
    if: contains(inputs.workflows_to_run, 'dependency-track-syft') || contains(inputs.workflows_to_run, 'all')
    runs-on: ${{ inputs.runner }}
    outputs:
      use_yarn4: ${{ steps.check.outputs.use_yarn4 }}
    steps:
      - uses: actions/checkout@v4
      - id: check
        run: |
          if grep -q '"packageManager": "yarn@4' package.json 2>/dev/null; then
            echo "use_yarn4=true" >> $GITHUB_OUTPUT
          else
            echo "use_yarn4=false" >> $GITHUB_OUTPUT
          fi

  run-tests:
    if: contains(inputs.workflows_to_run, 'run-tests') || contains(inputs.workflows_to_run, 'all')
    uses: ./.github/workflows/app-test.yml
    with:
      runner: ${{ inputs.runner }}

  #skip if private repo
  codeql-scan:
    if: contains(inputs.workflows_to_run, 'codeql-scan') || contains(inputs.workflows_to_run, 'all') && github.event.repository.private == false
    uses: ./.github/workflows/codeql.yml
    with:
      languages: ${{ inputs.languages }}
      build_mode: ${{ inputs.build_mode }}
      runner: ${{ inputs.runner }}

  build-check:
    if: contains(inputs.workflows_to_run, 'build-check') || contains(inputs.workflows_to_run, 'all')
    uses: ./.github/workflows/bundlemon-build-size.yml
    with:
      branch_name: ${{ inputs.bundlemonrc_branch_name }}
      runner: ${{ inputs.runner }}

  dependency-track-yarn4:
    needs: detect-project-type
    if: needs.detect-project-type.outputs.is_node == 'true' && (contains(inputs.workflows_to_run, 'dependency-track-yarn4') || contains(inputs.workflows_to_run, 'all'))
    uses: ./.github/workflows/dependency-track-yarn4.yml
    secrets:
      DTRACK_API_KEY: ${{ secrets.DTRACK_API_KEY }}
    with:
      runner: ${{ inputs.dependency_track_runner }}

  dependency-track-syft:
    needs: [detect-project-type, detect-yarn4]
    if: needs.detect-project-type.outputs.is_node == 'true' && needs.detect-yarn4.outputs.use_yarn4 == 'false' && (contains(inputs.workflows_to_run, 'dependency-track-syft') || contains(inputs.workflows_to_run, 'all'))
    uses: ./.github/workflows/dependency-track-syft.yml
    secrets:
      DTRACK_API_KEY: ${{ secrets.DTRACK_API_KEY }}
    with:
      runner: ${{ inputs.dependency_track_runner }}
